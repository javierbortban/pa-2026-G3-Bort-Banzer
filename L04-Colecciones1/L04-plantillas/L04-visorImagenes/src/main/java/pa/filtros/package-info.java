/**
 * Contiene las clases que implementan los distintos filtros de imagen.
 * <p>
 * Cada clase dentro de este paquete define un filtro concreto (por ejemplo,
 * suavizado, detección de bordes o inversión de color) que puede aplicarse
 * a las imágenes mostradas en el visor.
 *
 * @author Javier
 * @version 1.0
 */
package pa.filtros;
